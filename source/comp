#!/bin/bash

FIL=$@

F77=ifort
#OPT='-openmp -parallel -fpp -traceback -check all -integer-size 64 -c'
OPT='-O2 -openmp -parallel -integer-size 64 -fp-model strict -no-ftz -ipo -c'

set echo

echo Compiling $FIL with $F77 with options $OPT ...

$F77 $OPT $FIL

echo Done.

unset echo

